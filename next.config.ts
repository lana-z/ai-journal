import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    // appDir is now the default in newer Next.js versions
    // and no longer needs to be specified as an experimental feature
  },
};

export default nextConfig;
